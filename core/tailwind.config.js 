/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./templates/**/*.html",
    "./**/templates/**/*.html",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          accent: '#a0522d',   // кофейный акцент
          light: '#f5e6d3',    // светлый кофейный
          dark: '#6b3a1f',     // тёмный кофейный
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],   // <-- пустой массив (без require(...))
}